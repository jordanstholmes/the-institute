version: 2
jobs:
  build:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run:
          name: Install node dependencies
          command: npm install
  test:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run:
          name: Test
          command: npm test
