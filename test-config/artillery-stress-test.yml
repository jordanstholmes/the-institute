config:
  target: 'http://localhost:7000'
  phases:
    - duration: 60
      arrivalRate: 100
  payload:
    path: './video_ids.csv'
    fields:
      - 'id'
scenarios:
  - flow:
    - get:
        url: "/videos/{{ id }}"